<%= form_for(@task, url: tasks_new_path) do |f| %>

<%= f.label :action %>
<%= f.select(:action, ['-','Clean', 'Vacuum', 'Fold']) %>

<%= f.label :room %>
<%= f.select(:room, ['-','kitchen', 'living room', 'dining room', 'bathroom', 'hallway', 'music room', 'all']) %>

<%= f.label :surface %>
<%= f.select(:surface, ['-','counters', 'bookshelves', 'sink']) %>

<% end %>
